//Init namespaces
var Phaser = Phaser || {};
var ss = ss || {};
ss.platformer = ss.platformer || {};
ss.platformer.gameobject = ss.platformer.gameobject || {};

(function () {
  "use strict";
  ss.platformer.gameobject.EntityFactory = function (game) {
    this.game = game;
  }
  .extend({
    createEntity: function (object) {
    }
  });
}());